{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "Launch",
        "preLaunchTask": "CMake: build",
        "program": "${workspaceFolder}/bin/pongc",
        "args": [],
        "cwd": "${workspaceFolder}"
    }
    ]
}